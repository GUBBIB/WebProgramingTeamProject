
# 프로젝트 작업 목록

MainPage.jsx
```jsx
44번째 줄 -> details: user, // userDetails를 user로 수정
```
```jsx
60~ 65번쨰 줄 -> 
// 검색 핸들러 함수 추가
  const handleSearch = (query) => {
    // 검색 로직 구현
    console.log(`Searching for ${query} in ${searchType}`);
    // 필요한 검색 로직 추가
  };
```
Signup.jsx
```jsx
29번째 줄 -> const res = await fetch(`${API_BASE_URL}/register`, { //api 경로 `${API_BASE_URL} 추가
```

